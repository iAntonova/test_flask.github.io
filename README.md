# test_flask.github.io

for WIN:
    py -3 -m venv .venv
    .venv\scripts\activate

    pip install werkzeug etc..

    DB:
    python
    from app import db
    db.create_all()
    exit()

    GITignore:
    